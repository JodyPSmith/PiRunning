# PiRunning
Raspberry Pi based python server to control Nordic track T 6.5 SI

## install & command list
 - Prep a micro SD card with "Raspberry Pi OS Lite x64"
 - Login over ssh with username and password from set up
 > sudo su -
 > passwd root
 - enter new password twice
 > apt-get update
 > apt install python3-gpiozero vim

## Git setup

## python setup
> python -m pip install --upgrade pip
> python -m venv .venv




goto folder and activate environment (after boot, need to automate?) 
> source .venv/bin/activate
> fastapi run server.py

  from fastapi.staticfiles import StaticFiles

      1  passwd root
    2  apt-get update
    3  pwd
    4  cd /opt
    5  ls
    6  mkdir sudo apt install python3-gpiozero
    7  ls
    8  rm -rf apt
    9  rm -rf install
   10  rm -rf python3-gpiozero/
   11  rm -rf sudo
   12  ls
   13  sudo apt install python3-gpiozero vim
  
   14  apt-get install vim
   15  mkdir gpio
   16  cd gpio/
   17  vim server.py
   18  python3 server.py
   19  vim fade.pw
   20  python3 fade.py
   21  mv fade.pw fade.py
   22  python3 fade.py
   23  vim server.py
   24  python3 server.py
   25  python -m pip install --upgrade pip
   26  ls
   27  python -m venv .venv
   28  ls
   29  ls -la
   30  source .venv/bin/activate
   31  which python
   32  python -m pip install --upgrade pip
   33  echo "*" > .venv/.gitignore
   34  pip install "fastapi[standard]"
   35  ls
   36  vim server.py
   37  python server.py
   38  vim server.py
   39  python server.py
   40  ls
   41  vim server.py
   42  deactivate
   43  source .venv/bin/activate
   44  which python
   45  fastapi dev server.py
   46  fastapi run server.py
   47  ls
   48  history
    49  vim server.py
   50  which python
   51  pip install python3-gpiozero
   52  pip install gpiozero
   53  vim server.py
   54  source .venv/bin/activate
   55  fastapi run server.py
   56  cd ..
   57  ls
   58  ls
   59  git clone https://github.com/JodyPSmith/PiRunning
   60  cd PiRunning/
   61  ls
   62  npm install
   63  curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
   64  \. "$HOME/.nvm/nvm.sh"
   65  nvm install 24
   66  node -v
   67  npm -v
   68  npm install
   69  node server.js
   70  vim .env
   71  node server.js
   72  git pull
   73  node server.js
   74  cd ..
   75  ls
   76  cd gpio/
   77  ls
   78  python server.py
   79  fastapi run server.py
   80  git pull
   81  vim server.py
   82  fastapi run server.py
   83  vim server.py
   84  fastapi run server.py
   85  vim server.py
   86  fastapi run server.py
   87  vim server.py
   88  fastapi run server.py
   89  vim server.py
   90  fastapi run server.py
   91  vim server.py
   92  fastapi run server.py
   93  vim server.py
   94  fastapi run server.py
   95  vim server.py
   96  fastapi run server.py
   97  pip install gpiozero rpi-gpio
   98  fastapi run server.py
   99  vim server.py
  100  fastapi run server.py
  101  fastapi run server.py
  102  history

   ## node install
   https://nodejs.org/en/download

   
